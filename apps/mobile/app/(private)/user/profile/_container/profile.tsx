import { View } from "react-native";

import ProfileSection from "@/components/section/private/user/profile/page-section";

const ProfileContainer = () => {
  return (
    <View className="w-full min-h-screen">
      <ProfileSection />
    </View>
  );
};

export default ProfileContainer;
