import { ScrollView, View, Text } from "react-native";
const PolicySection = () => {
  return (
    <ScrollView className="w-full min-h-screen bg-background">
      <View className="items-center w-full px-6 ">
        <Text>Init term screen</Text>
      </View>
    </ScrollView>
  );
};

export default PolicySection;
