import { ScrollView, Text,View } from "react-native";

const HomeAdminSection = () => {
  return (
    <ScrollView className="relative">
      <View className="">
        <Text>Initial Home Admin Section</Text>
      </View>
    </ScrollView>
  );
};

export default HomeAdminSection;
