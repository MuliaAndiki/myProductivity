import { ScrollView, View, Text } from "react-native";

const HomeUserSection = () => {
  return (
    <ScrollView className="relative">
      <View className="">
        <Text>Initial Home User Section</Text>
      </View>
    </ScrollView>
  );
};

export default HomeUserSection;
